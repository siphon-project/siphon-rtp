# siphon-rtp-hep — HEP3 (Homer Encapsulation Protocol v3) packet encoding for QoS/RTCP telemetry
# export to a Homer capture node. Pure Rust, zero dependencies (std only).
[package]
name = "siphon-rtp-hep"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "siphon-rtp HEP3 (Homer) telemetry — RTCP/QoS capture encoding, MOS, and UDP export"
readme = "README.md"
keywords = ["hep", "homer", "rtcp", "telemetry", "voip"]
categories = ["network-programming"]

[dependencies]
tokio = { workspace = true }
